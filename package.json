{
  "name": "os_scheduler",
  "version": "1.0.0",
  "description": "OS processes scheduler electron project",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "upgrade": "rm -rf node_modules && npm install",
    "build:prod": "rm -rf build && webpack --env prod",
    "build:dev": "rm -rf build && webpack --colors --watch --progress --env dev"
  },
  "author": "Ahmed Karaman",
  "license": "ISC",
  "dependencies": {
    "apexcharts": "^3.6.6",
    "electron": "^4.1.4"
  },
  "devDependencies": {
    "eslint": "^5.16.0",
    "webpack": "^4.30.0",
    "webpack-cli": "^3.3.0"
  }
}
